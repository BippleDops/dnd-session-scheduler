'use client';

export default function Footer() {
  return (
    <footer className="text-center py-6 text-xs text-[var(--ink-faded)] mt-auto hidden md:block">
      <div className="sword-divider"><span>⚔️</span></div>
      <p className="mt-2 font-[var(--font-heading)] tracking-wide">D&D Session Scheduler — Self-Hosted</p>
    </footer>
  );
}

