{
  "name": "dnd-session-scheduler-api",
  "version": "1.0.0",
  "description": "D&D Session Scheduler — API server",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate-from-sheets.js",
    "test": "jest",
    "test:ci": "jest --ci --forceExit --detectOpenHandles",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "compression": "^1.8.1",
    "dotenv": "^16.4.7",
    "express": "^4.22.2",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "googleapis": "^178.0.0",
    "helmet": "^8.0.0",
    "node-cron": "^4.6.0",
    "nodemailer": "^10.0.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
